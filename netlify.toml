# Netlify configuration for TanStack Start + Nitro SSR app
# Nitro (netlify preset) handles function routing automatically.

[build]
  command = "npm run build"
  publish = "dist"

# Nitro generates its own _headers and _redirects files in dist/.
# Do NOT add manual redirects/headers here or they will conflict.
